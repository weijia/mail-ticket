============
Installation
============

At the command line::

    $ easy_install mail_ticket

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mail_ticket
    $ pip install mail_ticket